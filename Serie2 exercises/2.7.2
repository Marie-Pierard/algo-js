const readlineSync = require("readline-sync");
let n = new Number(readlineSync.question("Enter a number"));
let m=null;
let i=1; 
while (i<=n){
    let m = new Number(readlineSync.question("Enter another number"));
    i+1;
}
